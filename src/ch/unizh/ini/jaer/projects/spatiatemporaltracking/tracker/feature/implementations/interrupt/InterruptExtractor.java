/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package ch.unizh.ini.jaer.projects.spatiatemporaltracking.tracker.feature.implementations.interrupt;

/**
 *
 * @author matthias
 * 
 * This type of extractor does not extract an information on its own Instead of
 * that it monitors the state of the algorithm and causes an interrupt if the
 * algorithm exceeds a given timestamp.
 */
public interface InterruptExtractor {
    
}
