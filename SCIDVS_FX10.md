# SciDVS on FX10 — jAER support

jAER hardware interface for the SciDVS camera driven by the Infineon EZ-USB
FX10 (CYUSB4014) controller, replacing the FX2/FX3 + ECP3 FPGA stack.
Firmware lives in the ModusToolbox project `~/mtw/scidvs_fx10`
(`scidvs_vendor.h` / `scidvs_vendor.c` are the authoritative protocol map).

**Device:** VID `0x152A` (Thesycon, same as all jAER cameras), PID `0x8420`.

## Classes

| Class | Role |
|---|---|
| `net.sf.jaer.hardwareinterface.usb.cypressfx3libusb.SciDVSFX10HardwareInterface` | `CypressFX3Biasgen` subclass: vendor requests, run/stop, mode select, event reader on bulk EP1-IN (0x81) |
| `eu.seebetter.ini.chips.davis.SciDVSFX10` | Chip class (subclass of `SciDVS`, 112x126, same `SciDVSConfig` bias set) |
| `LibUsb3HardwareInterfaceFactory` | Registers PID 0x8420 (unconditional, independent of `aer.mode.gaer`) |

## How to use

1. Make sure the udev rule for `152a:8420` is installed (see the fx10 session
   artifact `setup-fx10-udev.sh`) so libusb can open the device without root.
2. Build and launch:

   ```sh
   cd ~/git/jaer_scidvs/jaer
   ant jar              # rebuilds dist/jAER.jar
   ./jAERViewer_linux.sh
   ```

3. In the AEViewer window select the chip class `SciDVSFX10`
   (`AEChip` menu → select chip class; add
   `eu.seebetter.ini.chips.davis.SciDVSFX10` to the class list the first
   time). With the camera plugged in, the interface is found by the libusb
   factory automatically; opening it starts streaming (jAER sends
   `VR_RUN_STOP=1` when the viewer starts acquisition).

## Sending biases

The full DAVIS-style coarse/fine bias panel from `SciDVSConfig` works
unchanged (`HW Configuration` tab). Every bias/config write goes out as
vendor request `0xBF VR_CHIP_CONFIG` with `wValue=moduleAddr`,
`wIndex=paramAddr` and a 4-byte value — identical framing to the FX3
`VR_FPGA_CONFIG`, with the SystemLogic2 module numbering preserved
(`FPGA_CHIPBIAS = 5` is the chip-bias module; bias addresses 0–36, chip
config 128–146, exactly as in `SciDVSConfig`).

**Current firmware caveat:** writes land in a shadow table on the device
(readable back via the same request) — they are *not yet applied to the
sensor*. The bias shift-register bit-bang is wired up once the sensor is on
the FMC connector. Out-of-range modules (≥8, e.g. the FPGA_USB/FPGA_ADC
writes from the generic DAVIS config path) are silently ignored by the
firmware; this is harmless.

## Capture modes (`VR_MODE_SELECT 0xC1`)

| Mode | Meaning |
|---|---|
| 0 | DVS only — synthetic raster-scan test pattern generated by the FX10 |
| 1 | APS only — synthetic (not handled by this interface yet) |
| 2 | G-AER capture — M0+ bit-bangs the G-AER handshake against the chip *or the FPGA emulator* (current bring-up setup) |

Default is mode 2 (persisted Java preference `SciDVSFX10.captureMode`;
`SciDVSFX10HardwareInterface.setCaptureMode(int)` changes it live). Use
mode 0 to sanity-check the USB path without a sensor: you should see a
clean raster sweep across the 112x126 array with alternating polarity.

## Event format / expected view

8-byte little-endian events on EP1-IN: word0 = 32-bit µs timestamp (used
directly as the jAER timestamp; wraps after ~71 min, no unwrap in v1),
word1 = `[31:28 type][27 pol][26:20 addr112][19:13 addr126][12:0 seq]`.
Only type `0x0` (DVS) is rendered; type `0xF` is DMA-buffer padding emitted
when the device ring underruns in G-AER mode and is silently skipped.
APS / IMU / timestamp-overflow types (0x1–0x6) are not handled in v1.

In the viewer the 112-valued address is jAER **x** and the 126-valued
address is jAER **y** (the SciDVS chip class is 112 wide × 126 tall).

Other caveats:

- `resetTimestamps` is a no-op (device timestamp is a free-running counter,
  no reset request in firmware v1).
- Recording works as usual (standard jAER `.aedat` via the logging button) —
  events are ordinary `PolarityEvent`s once extracted.
- `VR_STATISTICS` (0xC4) is exposed as
  `SciDVSFX10HardwareInterface.getStatistics()` → 8 uint32s:
  eventsSent, buffersSent, overflowCount, configWrites, run, mode,
  m0_alive, ringBuffers/fastBuffers.
