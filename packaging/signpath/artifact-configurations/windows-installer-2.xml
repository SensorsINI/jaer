<?xml version="1.0" encoding="utf-8"?>
<!--
  SignPath project "jaer" artifact configuration slug: windows-installer-2
  Paste this XML into that slug (Edit). Do not use inactivated slug windows-installer
  (v1 cannot be reused after the previous product-name mismatch).
  product-name must match install4j <application name="..."> PE ProductName exactly.
  Docs: https://docs.signpath.io/artifact-configuration/syntax
-->
<artifact-configuration xmlns="http://signpath.io/artifact-configuration/v1">
  <parameters>
    <parameter name="version" required="true" />
  </parameters>
  <pe-file path="jAER_windows-x64_*.exe"
           product-name="jAER - Desktop Application for Event Sensors"
           product-version="${version}">
    <authenticode-sign />
  </pe-file>
</artifact-configuration>
